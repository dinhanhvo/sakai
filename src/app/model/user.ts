import {n} from "@fullcalendar/core/internal-common";

export interface User {
    id?: number;
    name?: string;
    email?: string;
    role?: string;
}

export interface UIOption {
    label?: string;
    value?: number
}
